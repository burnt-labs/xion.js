# Generated AA API types
# NOTE: These files ARE committed to ensure deterministic builds.
# They should be regenerated when the AA-API contract changes.
# Use: pnpm generate:types
# CI validates these match the live API schema.

