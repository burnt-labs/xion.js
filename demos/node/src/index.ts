import { testnetChainInfo } from "@burnt-labs/abstraxion-js";

// Placeholder. Real Node demo scenarios (signer-mode account creation,
// programmatic signing) land later — see demos/node/README.md.
console.log("[abstraxion-js · node demo]");
console.log("chainId:", testnetChainInfo.chainId);
console.log("rpc:    ", testnetChainInfo.rpc);
console.log("rest:   ", testnetChainInfo.rest);
