---
"@burnt-labs/abstraxion-react-native": major
---

feat(abstraxion-react-native): rewire onto the framework-agnostic `@burnt-labs/abstraxion-js` controllers/strategies instead of the legacy in-package implementation. Wires the new methods through the React Native hooks and refreshes the bundled demo (Expo SDK 54 / React Native 19).
