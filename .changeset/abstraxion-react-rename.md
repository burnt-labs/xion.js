---
"@burnt-labs/abstraxion-react": major
---

refactor(abstraxion-react): rename from `@burnt-labs/abstraxion`. The React package keeps the JSX/provider/hooks surface; framework-agnostic controllers, strategies, and signing logic move out to the new `@burnt-labs/abstraxion-js` package.
